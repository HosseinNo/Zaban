<?php
// اگر مستقیم از مرورگر باز شود، چیزی برنگرداند
if (realpath(__FILE__) === realpath((string)($_SERVER['SCRIPT_FILENAME'] ?? ''))) { http_response_code(404); exit; }
/**
 * پیکربندی تاکورا.
 *
 * این فایل را به config.php تغییر نام دهید و مقادیر را پر کنید.
 * اگر پلسک اجازه می‌دهد، بهتر است در پوشهٔ private/ بالاتر از httpdocs
 * بگذاریدش با نام talkora-config.php — آن‌وقت اصلاً از وب قابل خواندن نیست.
 *
 * ⚠ config.php را هرگز در گیت نگذارید.
 */

return [
    // ── پایگاه داده (از پلسک: Databases → اطلاعات اتصال) ──────────
    'db_host' => 'localhost',
    'db_name' => 'talkora',
    'db_user' => 'talkora',
    'db_pass' => '',

    // ── کلید HMAC کد یک‌بارمصرف ──────────────────────────────────
    // یک رشتهٔ تصادفی بلند. با این دستور بسازید:
    //   php -r "echo bin2hex(random_bytes(32));"
    'otp_pepper' => 'CHANGE-ME-به-یک-رشتهٔ-تصادفی-۶۴-کاراکتری',

    // ── sms.ir ───────────────────────────────────────────────────
    // پنل sms.ir → توسعه‌دهندگان → کلید API
    'smsir_api_key' => '',
    // پنل sms.ir → قالب‌های وریفای → شناسهٔ قالبی که ساخته‌اید
    'smsir_template_id' => 0,
    // نام متغیر داخل قالب. اگر در قالب نوشته‌اید #CODE#، اینجا هم CODE
    'smsir_param_name' => 'CODE',

    // true یعنی پیامک واقعی ارسال نشود و کد در لاگ خطای PHP بیفتد.
    // برای تست قبل از شارژ پنل مفید است. قبل از استفادهٔ واقعی false کنید.
    'sms_dry_run' => false,

    // ── قواعد امنیتی ─────────────────────────────────────────────
    'otp_ttl'          => 120,  // اعتبار کد، ثانیه
    'resend_cooldown'  => 60,   // فاصلهٔ ارسال دوباره، ثانیه
    'otp_max_attempts' => 5,    // تلاش اشتباه قبل از باطل شدن کد
    'rate_phone_hour'  => 5,    // سقف درخواست کد برای یک شماره در ساعت
    'rate_ip_hour'     => 20,   // سقف درخواست کد از یک IP در ساعت
];
