# تاکورا — یادداشت‌های کار روی این مخزن

## قاعدهٔ اول: همیشه از آخرین نسخهٔ گیت‌هاب شروع کن

مخزن بین چند نفر مشترک است. **پیش از هر تغییری** آخرین حالت را بگیر،
نه از روی نسخهٔ محلی:

```bash
git fetch --all --prune
git log --oneline HEAD..origin/<branch>     # چیزی که ندارم
git pull --rebase origin <branch>
```

اگر روی شاخهٔ دیگری کار شده، همان را مبنا بگیر. هرگز روی نسخهٔ قدیمی
کامیت نزن و هرگز کار کسی را با force بازنویسی نکن.

شاخهٔ کاری فعلی: `claude/lingo-talk-saas-design-5fe1yq`

## معماری — چیزی که هست، نه چیزی که فرض می‌شود

بدون فریم‌ورک و بدون composer و بدون npm. دلیلش هاست اشتراکی پلسک است:
نه Node دارد، نه امکان build. پس:

- **PHP 8 خام** برای API (`panel/api/`)
- **جاوااسکریپت ساده** در یک فایل HTML برای هر پنل — بدون React، بدون
  Tailwind، بدون TypeScript، بدون بسته‌های npm
- **MySQL** بدون RLS؛ جداسازی آموزشگاه‌ها در کد و از راه `t_sql()` در
  `panel/api/_ctx.php` انجام می‌شود

اگر کسی کامپوننت React یا shadcn پیشنهاد داد، **طرحش** را بردار و با
همین ابزارها پیاده کن. افزودن React یعنی افزودن مرحلهٔ build که روی
این هاست اجرا نمی‌شود.

## قواعد محصولی که فراموش می‌شوند

- تاریخ‌ها میلادی ذخیره، شمسی نمایش داده می‌شوند (بند P.2)
- «فرد» = شنبه، دوشنبه، چهارشنبه · «زوج» = یکشنبه، سه‌شنبه (بند P.3)
- پیامک از سرویس **وریفای** sms.ir می‌رود، نه ارسال معمولی (بند P.4) —
  خط خدماتی به کسی که «لغو تبلیغات» زده هم می‌رسد
- پول به **ریال** و صحیح ذخیره می‌شود، به **تومان** نمایش داده می‌شود (بند P.5)
- هیچ ارجاع به دامنهٔ خارجی در HTML نباشد؛ `build-dist.py` جلویش را می‌گیرد

## فایل‌هایی که هرگز کامیت نمی‌شوند

`panel/api/config.php` و `private/talkora-config.php` — رمز پایگاه داده
و کلید امضای کدهای ورود داخل‌شان است.

## قبل از تحویل

```bash
python3 build-dist.py        # خودش چند بررسی دارد و روی خطا متوقف می‌شود
```
